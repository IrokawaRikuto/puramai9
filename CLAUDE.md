# 神ゲー創造主エボリューション 企画書プロジェクト

±9(プラマイ・ナイン)の企画・ルール・プロトタイプの進捗を記録する。

提出先:**神ゲー創造主エボリューション**(NHKエンタープライズ主催。提出物=A4横4ページPDFの企画書＋任意で2分動画)
チーム名:**Noad**(5名 / Never Overcome All Difficulties)

最終出力:`プラマイナイン_企画書.pdf`(提出用4ページ清書) / `プラマイナイン_企画書.pptx`(編集用)

---

## ⚠ 重要:2026-06-17 ルール再設計(旧→新)

このプロジェクトは **大規模なルール再設計** を経た。ドキュメント一式(`ルールブック.md`・`企画書草案.md`・本ファイル)に加え、**`prototype/` の実装コードと資料生成スクリプトも新ルールへ移行済み**(2026-06-23)。
※ `puramai9/`(Unity 人vsCPU の別プロジェクト)は本リポジトリ未取り込み。取り込み後、`prototype/Sim/Core/` を差し替えれば同様に移行できる。

### 旧 → 新 の主な変更

| 項目 | 旧 | 新 |
|---|---|---|
| コインのタイミング | 前半と後半の **間** に投げる(見てから後半で調整) | **最初に投げる**。当たり偶奇を公開してから手を組む |
| 送り先判定 | コイン偶奇 × 合計偶奇の一致 | 同左(ロジック不変)。ただし当たり偶奇は**先出し公開** |
| 手札 | 7枚・**上限なし**・0枚出し(溜め込み)可 | **6枚**・出すのは**前半2枚以上/後半1枚以上**・溜め込み廃止 |
| ドロー報酬 | なし | **ゾロ目ドローボーナス**(2枚+1/3枚+2/補充型/上限8) |
| 5枚一致 | なし | 絶対値9化=**±45の特大の一撃**(自爆あり) |
| 終盤 | 山札管理依存(基本切れない) | **エスカレーション**(全体倍率↑で加速) |
| HP | 数値表示 | **非公開**(ゲージ+色で大体だけ) |
| 破棄 | 「破棄」 | **チャージ**に改名(累計非表示・行為は可視) |
| アイテム | **5種** | **3種**に再編(下記) |

### アイテム再編(5種 → 3種)

- 残す/差し替え:**①コイン反転**(後半終了後判明に変更)/ **②符号反転**(新規・向き軸)/ **③ダブルシフト±7**(旧⑤)
- 廃止:旧②序盤ドローブースト(ゾロ目と被り)/ 旧③強制リシャッフル(溜め込み廃止で不要)/ 旧④絶対値ブースト+8(②符号反転と差し替え)

---

## 📌 現在の到達点(2026-06-17 時点)

### 企画書(ドキュメント)— 新ルールで更新済

| 成果物 | 状態 | 備考 |
|---|---|---|
| `プラマイナイン_企画書.pdf` | ✅ 最新 | **提出用A4横4ページ清書**(神エボ要項準拠) |
| `プラマイナイン_企画書.pptx` | ✅ 最新 | 上記の編集用ソース |
| `ルールブック.md` | ✅ 最新・正本 | 新ルールを網羅(コア+多人数) |
| `企画書草案.md` | ✅ 最新 | 作業用下書き(新ルール+世界観) |
| `プラマイナイン_企画書.txt` | ✅ 最新 | 提出用テキスト(新ルール) |
| `プラマイナイン_企画書資料.pptx`(旧12枚) | ⚠ 旧版 | 旧ルールのレガシー。4ページ清書pdfに置き換え。参照しない |

### プロトタイプ — ✅ 新ルールへ移行済み(2026-06-23)

`prototype/`(純C#ルールエンジン+CPU検証+Unity人vsCPU)を新ルールへ全面移行。
- 反映:コイン先出し / 手札6・前半2後半1出し・溜め込み廃止・持ち越し / ゾロ目ドローボーナス(上限8) / 5枚一致±45 / エスカレーション(`EscalationMult`:5Rまで×1・以降3Rごと+×1) / HP非公開(ゲージ+色) / 破棄→チャージ(累計非表示) / アイテム3種(①コイン反転・②符号反転・③ダブルシフト±7)。旧②③④アイテムを削除
- 資料生成スクリプトも書き換え:`gen_mockup.py`(新ルール・Linuxフォント対応)・`gen_pptx.py`(12枚資料)・`update_onesheet*.py`
- ※ `puramai9/`(Unity別プロジェクト)は未取り込み。取り込み次第 `Core/` を差し替えれば移行可能。

### 次の一手(候補)

1. **プロトを新ルールへ移行**(下記チェックリスト)→ Unityで手触り確認
2. 新ルールでバランス再計測 → コスト(6/3/2)・±7・エスカレーション段階・奇偶配分を詰める
3. ドラッグ&ドロップ・エフェクト(演出哲学:情報ミニマル/演出過剰)
4. 多人数 / オンライン(Photon・UGS)へ拡張
5. 提出用2分動画(動くプロト=制作力アピール)

---

## プロト移行チェックリスト(旧ルール実装 → 新ルール)— ✅ 1v1分は完了(2026-06-23)

- [x] **コインを最初に投げる**:`PlayTurn`/Unity `Turn()` を「コイン公開 → 前半 → 後半」に組み替え。`DecideFront` にコインを渡す
- [x] **手札6枚化**:初期6 / ラウンド頭に「6＋ゾロ目ボーナス」まで補充 / 上限8(`HandBase`/`HandCap`)
- [x] **出し下限**:前半2枚以上・後半1枚以上を強制(Unityは確定ボタンで弾く / CPUは2枚・1枚を保証)。溜め込み廃止
- [x] **持ち越し**:出さなかったカードは手札に残り次ラウンドへ持ち越し(補充ロジックで自然に実現)
- [x] **ゾロ目ドローボーナス**:`ZoroDrawBonus`(2枚→+1 / 3枚→+2 / 4枚以上→+3)。上位のみ・補充型・上限8
- [x] **5枚完全一致**:`FiveMatch` で同数字5枚→±45(自爆あり)
- [x] **エスカレーション**:`EscalationMult`(5Rまで×1・以降3Rごと+×1)をHP反映の倍率に
- [x] **HP非公開**:Unity/モックアップともHP数値を隠し、ゲージ+色(＋寄り=青/−寄り=赤)に変更
- [x] **チャージ改名**:`Charge`(旧「破棄」)。累計は自分のみ可視・相手は非表示
- [x] **アイテム3種化**:`ItemType { None, CoinFlip, SignFlip, DoubleShift }`。①は後半終了後に判明・②符号反転(新規)・③±7。旧②③④削除
- [x] 旧③強制リシャッフル関連の処理(`_pendingReshuffle`/`DoReshuffle` 等)を**全削除**
- [x] 旧④絶対値+8(`AbsBoost`)の計算を削除し、②符号反転(符号のみ反転=偶奇不変)を実装
- [ ] 多人数の敗北者妨害役を①②③に更新(※エンジンは現状1v1のみ。多人数は今後)

---

## ゲームタイトル(仮)
**±9 (プラマイ・ナイン)**

## ゲームコンセプト
運(コイン)・計算(数字)・読み合い(心理戦)が一手に同居する、
運命を賭ける二人対戦の心理戦カードバトル。
性格づけ:実力で勝つゲーム、運は確実性を少し奪うスパイス。

---

## 確定したルール(新)

> **詳細は `ルールブック.md` を正とする。** 本節は要点のみ。

### デッキ(共通・1つ・テキストレス)
- ブルー(数字)-9〜9 × 各5枚 = 95 / レッド19(×2:3 ×-2:3 ×3:2 ×-3:2 ×-1:5 ±5:4)/ ゴールド±9 ×1 = **115枚**
- レッド単体(ブルー無し)=0 / **±5は最終合計の絶対値を+5(最後に計算・符号は最終合計に従う・合計0なら無効)** / 除算なし
- ゴールド=全カード±9化(最大±45・**自爆あり**)
- 裏面共通(ブラフ)/ 同数字は各5枚=ゾロ目は希少
- ⚠ 奇偶カードの配分はテスト調整

### コイン・送り先
- **ラウンド頭にコインを投げ、当たり偶奇(奇/偶)を公開**してから手を組む
- 自分の最終合計の偶奇が当たりと **一致→相手へ / 不一致→自分へ(自爆)**。合計0は無効

### 1ラウンドの流れ
1. ドロー(手札6へ＋ゾロ目ボーナス、上限8。山札P×5以下で先にリシャッフル)
2. **コイン投げ→当たり偶奇公開**
3. 前半(**2枚以上**伏せる＋チャージ可)
4. 前半アイテム開示
5. 後半(**1枚以上**伏せる。①コイン反転は後半のみ)
6. オープン→最終合計→送り先判定→HP反映(エスカレーション倍率)→勝敗

### 手札・出し方
- 手札6 / 出すのは前半2枚以上・後半1枚以上(最低3枚)/ 残りは持ち越し
- **0枚出し(溜め込み)は廃止**

### ゾロ目ドローボーナス
- 同数字2枚→+1 / 3枚→+2 / 4枚以上→+3(仮)。枚数のみ・上位適用・補充型・上限8
- 5枚一致→絶対値9化(±45・自爆あり)

### 計算
- 最終合計=前半合計+後半合計。乗算は同フェイズの数字のみ。足し算先・除算なし
- ブルー無しフェイズ=0 / 最終合計0=無効

### 終盤・情報
- **エスカレーション**:ラウンド経過で全体倍率↑(間延び防止＋終盤ドラマ)
- **HP非公開**:ゲージ＋色(＋=青/−=赤)で大体だけ

### 勝利条件(0/200)
- 相手を0か200にしたら勝ち / 自分が端で負け
- 両者同時着弾(1v1)→引き分け→オーバータイム(両者HP50・先に100か0で負け)

---

## チャージ(コスト)
- 旧「破棄」を改名。出すカードとは別枠で **1ラウンド1枚** 消費しコストを貯める
- 所持アイテムは開始時に種類公開 / **チャージ累計は非表示**(数えれば読める)
- 手札6が「出す/残す(ゾロ目)/チャージ」で取り合いになる

---

## アイテムシステム(3種・チャージ制)

試合前に1個持ち込み・開始時に種類公開・**1ラウンド1回**使用。

| # | アイテム | 軸 | コスト | 開始 | タイミング | 効果 |
|---|---|---|---|---|---|---|
| ① | コイン反転 | 送り先 | **6** | 2 | 後半のみ | 当たり偶奇を奇⇄偶へ反転(**後半終了後に判明**=後出し) |
| ② | 符号反転 | 向き | **3** | 1 | 前半/後半 | 自分の最終合計の符号を反転(偶奇は不変) |
| ③ | ダブルシフト | 全体かく乱 | **2** | 1 | カード出し中 | 全員の合計に同符号±7(多人数で本領) |

- ①への対抗=弱い手・逆偶奇で受け流す(ローリスク選択がそのまま防御)
- ⚠ コスト額・開始コスト・±7は **暫定値**(プレイテストで調整)

---

## モード構成(3種)

| 項目 | カスタム | カジュアル | ガチ対戦 |
|---|---|---|---|
| 参加 | ルームコード | 自動マッチング | 自動マッチング(レート) |
| 形式 | 1v1/2v2/1v1v1/1v1v1v1 | バトロワ | 1v1のみ |
| アイテム | ON/OFF | ON | ON |
| 初期HP | 設定可 | 人数準拠 | 100 |
| 捨て札開示 | ON/OFF | ON | OFF |
| ターン時間 | 設定可 | 50秒 | 30秒 |
| レート | なし | なし | あり(初期1000P) |

### 多人数(2v2/1v1v1/1v1v1v1)
- 攻撃は敵全員へ / 送り先判定は1v1同様 / 初期HP人数準拠(100/150/200)・可動域0〜初期×2
- アイテムコストは個人ごと / **③ダブルシフト±7は敵味方全員**
- チーム=2人とも敗北で負け / バトロワ=最後の1人まで
- 敗北者=手札を捨て・ドローなし・毎ターンコスト+1・アイテムのみ使用可(妨害役)
- ※ 旧③強制リシャッフルは廃止 → 「味方巻き込みリシャッフル」も無し

---

## 残課題 / 未確定事項

### テスト調整(机上で決めない)
- [ ] **奇偶カードの配分**(偶奇合わせを毎ラウンド少し悩ましく/偶数偏重の平準化)←最重要
- [ ] アイテムのコスト(①6/②3/③2)・開始コスト(①2/他1)・③の±7
- [ ] エスカレーションの段階(何ラウンド目で×2か)
- [ ] ゲージの解像度(HP非公開の粗さ)
- [ ] 手札持ち越し上限 / ゾロ目ボーナス上限(現8)/ 5枚一致の報酬詳細
- [ ] ①コイン反転の可視性(行為のみ可視→理不尽なら明示ゲージに格上げ)
- [ ] ガチ対戦のレート変動値

### 保留(未決定の論点)
- [ ] **前半の開示**:後半を「相手の前半を見て反応する窓」にする案。①の後出し設計と衝突するため未決定

### プロト移行(上記チェックリスト参照)
- [x] `prototype/`(C#エンジン+Unity)を新ルールへ移行(2026-06-23)
- [ ] `puramai9/`(Unity別プロジェクト)を取り込み→ `Core/` 差し替えで移行
- [ ] 多人数(2v2/バトロワ)対応をエンジンに追加

---

## 進捗ログ

- **ルールの曖昧点を裁定・全反映(2026-06-24)** → 未定義だった点を確定し、`ルールブック.md`・`企画書草案.md`・実装・資料スクリプトへ反映。
  - **±5**:フェイズ内では計算せず、**最終合計に最後に適用して絶対値を+5**(複数枚は枚数×5・符号は最終合計に従う・合計0なら無効)。エンジン/CPU/Unityを `ApplyPlus5` に統一
  - **オーバータイムのエスカレーションは一律×5**(`ResolveAfterFront` で `Overtime ? 5 : EscalationMult`)
  - **前半の先行開示はしない**(全カードを最後に同時オープン・①コイン反転も同時判明)→ §16.2保留を解消
  - **5枚一致の符号は揃えた数字に従う** / **同ターンにゴールドなら5枚一致は不発(ゴールド優先)** / **②符号反転はゴールド符号も反転**
  - **出せる札が無くても最低枚数(前半2・後半1)は強制**
- **プロト＆スクリプトを新ルールへ移行(2026-06-23)** → `prototype/`(C#ルールエンジン+CPU+Unity人vsCPU)と資料生成スクリプトを全面的に新ルールへ書き換え。
  - エンジン:コイン先出し / 手札6・前半2後半1・溜め込み廃止・持ち越し / ゾロ目ドローボーナス(上限8) / 5枚一致±45 / エスカレーション(`EscalationMult`) / HP非公開 / 破棄→チャージ / アイテム3種(①コイン反転・②符号反転・③ダブルシフト±7)。旧 `DrawBoost`/`Reshuffle`/`AbsBoost` を削除し `IAgent.DecideFront` にコインを追加
  - スクリプト:`gen_mockup.py`(新ルール・Linuxフォント対応で再生成)・`gen_pptx.py`(12枚資料)・`update_onesheet*.py` のキャプションを新ルール化
  - リポジトリへ `prototype/` 一式(Library等の生成物は除外)・`.gitignore` を追加
  - ⚠ dotnet 不在のためC#のビルド検証は未実施(構文は手検証)。`puramai9/` Unity別プロジェクトは未取り込み・多人数は今後
- **ルール再設計(2026-06-17)** → ドキュメント一式を新ルールへ全面更新。提出先を神ゲー創造主エボリューションに、チーム名をNoad(5名)に確定。
  - コイン先出し(当たり偶奇公開)/ 手札6・前半2後半1出し・溜め込み廃止 / ゾロ目ドローボーナス・5枚一致±45 / エスカレーション / HP非公開 / 破棄→チャージ(累計非表示)/ アイテム3種化(①コイン反転[後半終了後判明]・②符号反転[新]・③ダブルシフト±7)。旧②③④アイテム廃止
  - 提出用A4横4ページ清書 `プラマイナイン_企画書.pdf`(+編集用pptx)を作成。旧12枚資料pptxは置き換え
  - 反映先:`ルールブック.md` / `企画書草案.md` / `プラマイナイン_企画書.txt` / `CLAUDE.md`
  - ⚠ `prototype/`・`puramai9/` の実装は旧ルールのまま → 移行チェックリストを新設

### 旧ルール時代の履歴(参考・実装は旧仕様)

- **ルール大改訂(2026-06-15)**:共通コイン化 / 前半3後半2の2段階出し / 偶奇パリティ判定 / 勝利条件0・200化 / レッド特殊カード化
- **詳細資料pptx / ワンシート最新化(2026-06-16)**:旧12枚資料pptxを最新ルールと整合(当時)
- **アイテムをコスト制に刷新(2026-06-16)**:破棄でコスト獲得→支払って発動。①コイン反転コスト6・後半のみ・再発動可 / 旧③(偶数ターン自己ドロー)削除 / 旧④強制リシャッフル・旧⑤±7 を整備
- **開示タイミング・フェイズスコープ追加(2026-06-16)**:使用開示は各フェイズ終了時 / 効果は使ったフェイズ合計に作用
- **ブルー必須エッジケース確定(2026-06-16)**:ブルー無しフェイズは0
- **山札補充ルール確定(2026-06-16)**:残P×5枚以下で捨て札リシャッフル→ドロー
- **モード3種・多人数デフォルト・敗北者ルール確定(2026-06-16)**:カスタム/カジュアル/ガチ対戦 / 攻撃は敵全員 / HP人数準拠 / チーム=両者敗北で負け・BR=最後の1人
- **プロト着手:ルールエンジン+CPU(2026-06-16〜17)** → `prototype/`(純C#)・`puramai9/`(Unity 人vsCPU)。**旧ルールで動作**。手札5→7枚化・破棄は選択→確定式・命中先の色分け表示・③発動タイミングの正しい実装 等を当時反映
  - ※ これらは旧ルール基準の実装。新ルールへの移行は上記チェックリストで実施する
