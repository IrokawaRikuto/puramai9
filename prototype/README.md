# ±9(プラマイ・ナイン) プロトタイプ(新ルール)

ルールエンジン(純C#)を共有し、(1) .NETでバランス検証、(2) Unityで人 vs CPU 実プレイ、の2つで使う。

```
prototype/
├─ Sim/                         ← .NET 9 検証プロジェクト
│  ├─ Core/                     ← ★共有ルールエンジン(Unityへ流用するのはココ)
│  │  ├─ Cards.cs   … カード/デッキ(115枚)
│  │  ├─ Game.cs    … ゲーム状態・1ターン解決・チャージ・ゾロ目/エスカレーション・オーバータイム
│  │  ├─ Agent.cs   … IAgent(操作の抽象)
│  │  └─ GreedyAi.cs… CPU(簡易ヒューリスティック)
│  ├─ Program.cs               … 検証ランナー(Unityには入れない)
│  └─ Sim.csproj
└─ Unity/
   └─ PlusMinusNineGame.cs      … Unity人vsCPU(uGUIをコード生成)
```

> ⚠ このプロトは **新ルール**(2026-06-17 再設計)に準拠:コイン先出し / 手札6・前半2枚以上/後半1枚以上 /
> 溜め込み廃止 / チャージ(旧「破棄」)/ ゾロ目ドローボーナス / 5枚一致±45 / エスカレーション /
> HP非公開(ゲージ+色)/ アイテム3種(①コイン反転・②符号反転・③ダブルシフト±7)。

---

## 1) バランス検証(.NET・すぐ動く)

```
cd prototype/Sim
dotnet run -c Release
```

- サンプル1試合のログ + アイテム別 勝率マトリクス(各マッチアップ400戦)が出る。
- `Program.cs` の `M`(試行数)や `Items.Cost(...)`(`Core/Game.cs`)を変えて、コスト調整→勝率変化を即確認できる。
- 暫定値はすべて `Core/Game.cs` に集約:アイテムコスト(①6/②3/③2)・開始コスト(①2/他1)・
  ゾロ目ボーナス・`EscalationMult`(5Rまで×1・以降3Rごと+×1)。
- ※ CPUは簡易AIのため、バランス数値はあくまで暫定。

---

## 2) Unity 人 vs CPU(実プレイ版)

### セットアップ
1. **Unity Hub → New Project → Unity 6 (6000.0.70f1)** → テンプレートは **「2D (Built-In Render Pipeline)」** か「Universal 2D」。名前は任意(例 PlusMinusNine)。
2. **Project Settings → Player → Other Settings → Active Input Handling = 「Both」**(または Input Manager (Old))。
   - ※ uGUIのクリックと `Input`(Rキー)に必要。新Input System単独だと反応しません。
3. プロジェクトの `Assets/Scripts/` に、次をコピー:
   - **`Sim/Core/` フォルダ一式**(Cards.cs / Game.cs / Agent.cs / GreedyAi.cs)
   - **`Unity/PlusMinusNineGame.cs`**
   - ⚠ `Program.cs` と `Sim.csproj` は **入れない**(Unity用ではない)。
4. **▶ 再生するだけ**。`[RuntimeInitializeOnLoadMethod]` で自動起動するので、シーンにGameObjectを置く必要はありません(置いてもOK)。最初にアイテムを選んで対戦開始。

### 操作(新ルール)
- **開始時**:持ち込みアイテムを選択(CPUはランダム)。①②③の3種。
- **コイン公開**:ラウンド頭で当たり偶奇(奇/偶)が出る。これを見てから手を組む。
- **前半**:手札カードをクリックで選択(**2〜3枚**)→「前半 確定」。
- **後半**:カード選択(**1〜2枚**)→「後半 確定」。
- 「アイテム使用」=このターンに発動(①コイン反転は**後半のみ**・②符号反転と③ダブルシフトは前半/後半どちらでも)。「符号±」=③/ゴールドの符号。
- 「チャージ」ON→カードをクリックして「チャージ確定」(1ラウンド1枚・コスト+1)。
- 画面右に直近ログ。HPは**非公開**で、ゲージ+色(＋寄り=青/−寄り=赤)のみ表示。
- 決着後 **R キー**でもう一度。

---

## 既知の簡略化(プロト v0.2)
- ドラッグ&ドロップ未対応(クリック選択)。
- HPは非公開(数値は出さない)。CPUのチャージ累計も非表示=本来の「数えれば読める」運用に準拠。
- ゴールド+乗算カードの併用など稀なケースは簡略計算。
- CPUは簡易AI(②③の搦め手を活かしきれない)→ バランス数値は暫定。
- 多人数(2v2/バトロワ)・オンラインは未実装(エンジンはまず1v1)。
- C# は file-scoped namespace ではなくブロック namespace を使用(Unity全バージョンで安全)。

## 次の改善候補
- AI強化(②符号反転・③ダブルシフトを活かす方策)→ 再測定でアイテム調整
- ドラッグ&ドロップ・演出・SE(演出哲学:情報ミニマル/演出過剰)
- 多人数ルール、オンライン(Photon / Unity Gaming Services)
